# Grok-4

xAI'nin Grok modeli OpenAI uyumlu bir API kullanır, bu nedenle onu `OpenAILike` entegrasyon sınıfıyla kullanabilirsiniz.

```python
!pip install llama-index-llms-openai-like
```

```python
grok_api_key = "xai-xxxxxxxx"
```

```python
from llama_index.llms.openai_like import OpenAILike

llm = OpenAILike(
    model="grok-4-0709",
    api_base="https://api.x.ai/v1",
    api_key=grok_api_key,
    context_window=128000,
    is_chat_model=True,
    is_function_calling_model=False,
)

response = llm.complete("Merhaba Dünya!")
print(str(response))
```

    Merhaba Dünya! 🌍 Bu, her yerdeki programcılar için evrensel selamlamadır. Bugün seni buraya hangi macera getirdi? 😊